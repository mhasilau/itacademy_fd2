import { routes, paths } from './shared/constans/routes';

import './styles/style.scss';

window.onload = () => {
    const pathname = Object.values(paths).find( path => path === window.location.pathname);

    
}